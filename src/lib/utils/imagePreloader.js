// Ensure an image is ready befory displaying it
// (Load the image into memory regardless of whether it's visible or not)
export const preloadImage = (src) => {
  return new Promise((resolve, reject) => {
    const img = new Image();
    img.onload = () => resolve(img);
    img.onerror = reject;
    img.src = src;
  });
};
